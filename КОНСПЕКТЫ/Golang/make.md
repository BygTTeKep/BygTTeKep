make - это встроенная функция в go, предназначенная только для 3-ех типов
1) slice
2) map
3) chan
ее задача инициализировать внутренюю структуру данных, а не просто выделить память
Сигнатуры
```go
make([]T, len, cap)
make(map[K]V, hint)
make(chan T, buffer)
```
